# calculate_unix_permissions
Unix Permissions Calculator – An interactive Python script that asks for file type and read/write/execute permissions (y/n) for owner, group, and others, then outputs the equivalent symbolic (e.g., -rwxr-xr-x) and numeric octal (e.g., 755) permission codes.
